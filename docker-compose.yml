version: '3'
services:
  server:
    build: AvitoProject/
    ports:
      - "8080:8080"
    depends_on:
      - postgres
    restart: always
  postgres:
    build: Postgres/
    ports:
      - "5555:5432"

